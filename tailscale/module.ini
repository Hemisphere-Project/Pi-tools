[module]
name = tailscale
description = Tailscale VPN with RO-filesystem support
group = tailscale
platforms = pi,x86

[install]
script = yes

[files]
bins = tailscale-start
services = tailscale-start.service

[starter]
comment = Tailscale VPN (run 'tailscale up' once to authenticate)
service = tailscale-start
