[module]
name = hdmi-rehandshake
description = Re-negotiate the HDMI link with the explicit config.txt mode at boot (TVs that latch "mode not supported")
group = display
platforms = pi

[install]
script = yes
