[module]
name = hdmi-rehandshake
description = Re-negotiate the HDMI link with the explicit config.txt mode at boot (TVs that latch "mode not supported"), plus a daytime watcher for a display cycled after boot
group = display
platforms = pi

[install]
script = yes
