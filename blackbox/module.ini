[module]
name = blackbox
description = Persistent journal on /data (200 MB budget, 1 month) + one line of player state per minute (blackbox.log, 32 MB ceiling)
group = system
platforms = pi

[install]
script = yes
