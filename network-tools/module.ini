[module]
name = network-tools
description = WiFi profile management and network forwarding
group = network
platforms = pi,x86

[files]
bins = setnet uplink-fwd
services = setnet.service uplink-fwd@.service iface-off@.service

[starter]
comment = Network utilities
service = setnet
