[module]
name = rtpmidi
description = RTP MIDI (CoreMIDI compatible)
group = rtpmidi
platforms = pi,x86

[deps]
apt = alsa-utils

[files]
bins = rtpmidi
services = rtpmidi.service

[starter]
comment = RTP MIDI
service = rtpmidi
