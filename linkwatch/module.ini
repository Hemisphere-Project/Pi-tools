[module]
name = linkwatch
description = Sync-link watchdog (30 s): dongle slave with a dead data path -> driver reload; master with a stale station -> deauth; sync AP silent -> hostapd restart / driver reload
group = network
platforms = pi

[install]
script = yes
