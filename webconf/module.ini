[module]
name = webconf
description = Web configuration interface (port 4038)
group = web
platforms = pi,x86

[files]
bins = webconf
services = webconf.service
npm = yes

[starter]
comment = Web configuration UI
service = webconf
