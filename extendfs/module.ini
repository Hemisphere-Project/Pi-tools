[module]
name = extendfs
description = Auto-expand last partition on new drive
group = core
platforms = pi,x86

[deps]
apt = cloud-guest-utils

[files]
bins = extendfs
services = extendfs.service

[starter]
comment = Expand last partition if drive fingerprint changed
service = extendfs
