[module]
name = rorw
description = Read-only root filesystem with writable /data
group = system
platforms = pi,x86

[install]
script = yes

[files]
bins = ro rw fake-clock
services = fake-clock.service fake-clock-autosave.service
